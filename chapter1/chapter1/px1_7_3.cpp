#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<math.h>
#include<string.h>

int main() {
    double max, min,pre=0;
    for (double i = 0; pre >= 0; i+=1e10) {
        pre = i;
    }
    max = pre;
    printf("%lf", max);
    
    return 0;
}